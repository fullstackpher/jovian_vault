---
title: Vue3 Composition API最佳实践
type: snippet
category: vue
difficulty: intermediate
tags: [vue3, 代码片段]
---
## 🎯 使用场景
- Vue3组合式函数封装

## 📝 代码实现
```javascript
// 封装可复用的逻辑
export function usePagination(api) {
  const loading = ref(false)
  const data = ref([])
  const page = ref(1)
  
  const loadData = async () => {
    loading.value = true
    try {
      data.value = await api({ page: page.value })
    } finally {
      loading.value = false
    }
  }
  
  return { loading, data, page, loadData }
}

// 使用示例
const { loading, data, loadData } = usePagination(fetchUsers)
```

## 💡 说明
- 优点：逻辑复用、类型推断友好
- 注意点：避免在setup外部使用响应式变量


## 🔗 相关片段
- [[Vue组件封装模式]]
- [[TypeScript类型定义]]