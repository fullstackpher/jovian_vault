<%*
const title = await tp.system.prompt("卡片标题", "Virtual DOM");
const difficulty = await tp.system.prompt("难度 (easy/medium/hard)", "medium");
const tags = await tp.system.prompt("标签 (逗号分隔)", "面试,前端,Vue");
const tagsArray = tags.split(",").map(tag => tag.trim());
-%>
---
title: <%* tR += title %>
type: flashcard
difficulty: <%* tR += difficulty %>
review_dates: [<% tp.date.now("YYYY-MM-DD") %>, <% tp.date.now("YYYY-MM-DD", 6) %>, <% tp.date.now("YYYY-MM-DD", 20) %>]
tags: [<%* tR += tagsArray.map(tag => `"${tag}"`).join(", ") %>]
---

## ❓ 问题
<%* tR += title %>是如何工作的？和React有什么不同？

## 💡 答案
### <%* tR += title.split("-").pop().trim() %>的实现
1. **模板编译**：Vue模板 → render函数
2. **响应式依赖追踪**：精准更新受影响组件
3. **Diff算法优化**：双端比较，同层比较

### 与React的区别
| 方面     | Vue   | React                    |
| ------ | ----- | ------------------------ |
| 更新粒度   | 组件级   | 应用级                      |
| Diff策略 | 双端比较  | 单端比较                     |
| 优化方式   | 响应式跟踪 | PureComponent/React.memo |

## 🎯 关键点
- Vue的VDOM更轻量，更新更精准
- React的VDOM更通用，生态更丰富

## 🔗 相关概念
<%*
const concepts = await tp.system.prompt("相关概念 (逗号分隔)", "响应式原理,Diff算法");
const conceptsArray = concepts.split(",").map(concept => concept.trim());
for (const concept of conceptsArray) {
    tR += `- [[${concept}]]\n`;
}
%>