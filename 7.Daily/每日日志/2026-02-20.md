---
created: 2026-02-20 12:27
tags:
  - 周期/日
  - 日记
  - "#计划"
---
<< [[2026-02-19]] | [[2026-02-21]] >>

## 📋 ==待完成的任务==

```dataview
TASK
FROM "7.Daily"
WHERE !completed AND date(file.cday) < date(today)
```

## 🍅 今日聚焦

- [x] 安装本地ollma，部署本地大模型 ✅ 2026-02-20
- [x] 配置openclaw，选择本地ollma模型 ✅ 2026-02-20
- [x] 安装clawx并配置飞书 ✅ 2026-02-20

## 🗓 今日日志

- 08:00 - 08:10 起床 #orange
- 08:30 - 11:30 玩手机 #grey
- 11:30 - 12:30 吃午饭 #green
- 12:30 - 13:00 安装ollma模型工具平台，部署qwen3.5模型 #red
- 14:00 - 16:00 安装clawx并配置飞书，Claude更换本地ollama模型

## 💭 Thoughts

- ! [15:40] 更改Claude模型为本地Ollama minimax2.5:cloud模型

```dataviewjs
// 使用备用API列表，提高稳定性
const apis = [
  'https://v1.hitokoto.cn/?c=d&c=h&c=i&c=j',
  'https://hitokoto.cn/api?type=d',
  'https://api.shadiao.pro/chp'
];
let 一言 = "加载中...";
let 来源 = "佚名的";
let 作者 = "";

async function fetchWithTimeout(url, timeout = 3000) {
  const controller = new AbortController();
  const id = setTimeout(() => controller.abort(), timeout);
  try {
    const response = await fetch(url, { signal: controller.signal });
    clearTimeout(id);
    return response;
  } catch (e) {
    clearTimeout(id);
    throw e;
  }
}

try {
  let data = null;
  for (const api of apis) {
    try {
      const response = await fetchWithTimeout(api);
      data = await response.json();
      break;
    } catch (e) {
      continue;
    }
  }

  if (data) {
    一言 = data.hitokoto || data.text || "今日一言获取失败";
    来源 = data.from || data.from_who || "未知来源";
    作者 = data.from_who || '佚名的';
  } else {
    一言 = "今日一言服务暂时不可用";
  }
} catch (error) {
  console.error("获取一言失败:", error);
  一言 = "今日一言加载失败";
}

dv.paragraph(`> [!quote] ✨ 一言\n${一言} —— ​《${来源}》 · ${作者}`);
```

## 📝 今日创建的笔记

```dataview
List
FROM ""
WHERE file.cday = date(today)
SORT file.ctime asc
```


## ✏️ 今日编辑过的笔记

```dataview
List
FROM ""
WHERE file.mday = date(today)
SORT file.mtime asc
```
